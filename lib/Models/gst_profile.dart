// name, status, address, tax payer type, business type and date of registration
class GstProfile {
  final String id;
  final String name;
  final String status;
  final String address;
  final String taxPayerType;
  final String buissnessType;
  final String date;
  GstProfile(this.id, this.name, this.status, this.address, this.taxPayerType,
      this.buissnessType, this.date);
}
